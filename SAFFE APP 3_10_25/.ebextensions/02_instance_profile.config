# Note: For Node.js environments, we'll use the default instance profile
# and add S3 permissions via AWS Console or CLI after deployment
# CloudFormation intrinsic functions (!Ref) are not supported in .ebextensions for Node.js

option_settings:
  aws:autoscaling:launchconfiguration:
    IamInstanceProfile: aws-elasticbeanstalk-ec2-role